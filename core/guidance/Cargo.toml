[package]
name = "guidance"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
packs = { path = "../packs" }

[dev-dependencies]
pipeline = { path = "../../pipeline" }
tempfile = "3"
