# app

This directory will hold the Xcode project (SwiftUI app), which lands with milestone M2.
Until then it is a placeholder so the monorepo layout (`core/` Rust workspace, `pipeline/`
builders, `app/` iOS app) is complete.
